# Learnings

Правила общие, подключены отсюда:

@../rules/learning.md

Проектные уточнения:

- Журнал заведён 2026-09-06. Факты, найденные опытом раньше, лежат в планах
  `../plans/202606*` (разделы «Known facts») и сюда не переносятся.
- Ключи заголовка и названия разделов — как в правилах (`Found`, `Plan`,
  `Current`, `## Expected`, `## Actual`, `## Verified`, `## Do`), текст — по-русски.
- `Verified` для бота — команда проверки из корневого `AGENTS.md`, путь в
  `var/` или строка из `logs/<internal_name>/bot.log` либо `<chat_id>.log`.
